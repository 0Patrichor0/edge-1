apiVersion: config.porch.kpt.dev/v1alpha1
kind: PackageVariant
metadata: # kpt-merge: /example-repo
  name: edge-b-b-repo
  annotations:
    internal.kpt.dev/upstream-identifier: 'config.porch.kpt.dev|PackageVariant|default|example-repo'
spec:
  annotations:
    approval.nephio.org/policy: initial
  upstream:
    package: repository
    repo: catalog-distros-sandbox
    workspaceName: main
  downstream:
    package: edge-b-b-repo
    repo: mgmt
  injectors:
  - kind: WorkloadCluster
    name: edge-b
  pipeline:
    mutators:
    - image: ghcr.io/kptdev/krm-functions-catalog/set-annotations:v0.1.4
      configMap:
        nephio.org/cluster-name: edge-b
