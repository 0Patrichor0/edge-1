apiVersion: config.porch.kpt.dev/v1alpha1
kind: Repository
metadata: # kpt-merge: default/example-cluster-name
  name: regional32
  namespace: default
  annotations:
    internal.kpt.dev/upstream-identifier: config.porch.kpt.dev|Repository|default|example-cluster-name
    nephio.org/cluster-name: regional32
spec:
  content: Package
  deployment: true
  git:
    branch: main
    directory: /
    repo: http://172.18.0.200:3000/nephio/regional32.git
    secretRef:
      name: regional32-access-token-porch
  type: git
