apiVersion: config.porch.kpt.dev/v1alpha1
kind: PackageVariant
metadata: # kpt-merge: /example-configsync
  name: regiona2-configsync
  annotations:
    internal.kpt.dev/upstream-identifier: 'config.porch.kpt.dev|PackageVariant|default|example-configsync'
spec:
  annotations:
    approval.nephio.org/policy: initial
  upstream:
    package: configsync
    repo: catalog-nephio-core
    workspaceName: main
  downstream:
    package: regiona2-configsync
    repo: edge01
  injectors:
  - kind: WorkloadCluster
    name: regiona2
  pipeline:
    mutators:
    - image: ghcr.io/kptdev/krm-functions-catalog/set-annotations:v0.1.4
      configMap:
        nephio.org/cluster-name: regiona2
