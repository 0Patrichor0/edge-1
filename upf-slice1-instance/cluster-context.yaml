apiVersion: infra.nephio.org/v1alpha1
kind: ClusterContext
metadata: # kpt-merge: /cluster-context
  name: cluster-context
  annotations:
    config.kubernetes.io/local-config: "true"
    internal.kpt.dev/upstream-identifier: 'infra.nephio.org|ClusterContext|default|cluster-context'
spec:
  cniConfig:
    cniType: macvlan # kpt-set: ${cni-type}
    masterInterface: eth1 # kpt-set: ${master-interface}
  siteCode: edge-1 # kpt-set: ${cluster-name}
